'use strict';

/**
 * objective-indicator service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::objective-indicator.objective-indicator');
