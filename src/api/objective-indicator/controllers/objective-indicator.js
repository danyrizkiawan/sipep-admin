'use strict';

/**
 * objective-indicator controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::objective-indicator.objective-indicator');
