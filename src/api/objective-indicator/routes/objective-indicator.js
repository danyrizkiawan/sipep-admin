'use strict';

/**
 * objective-indicator router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::objective-indicator.objective-indicator');
