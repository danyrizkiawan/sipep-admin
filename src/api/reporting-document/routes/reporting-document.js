'use strict';

/**
 * reporting-document router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reporting-document.reporting-document');
